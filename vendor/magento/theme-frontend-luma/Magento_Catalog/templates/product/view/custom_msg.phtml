<?php
	$objectManager = \Magento\Framework\App\ObjectManager::getInstance();
	$product = $objectManager->create('Magento\Catalog\Model\Product')->load($block->getProduct()->getId());

	$vendor_id = $product->getData("udropship_vendor");

	$id = $vendor_id;
	if($id < 9){
	  $custId = "#00".$id;
	}else if($id > 9){
	  $custId = "#0".$id;
	}
	$customer_message = "This product has been brought to cibilia.com by cibilian ".$custId;
?>
<div><?php echo $customer_message; ?></div>
